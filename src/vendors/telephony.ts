/** Telephony adapter. Duration and recording come from the exchange when Prem connects it. */
export function exchangeDurationSeconds(_callId: string | null): number | null {
  return null;
}

export function recordingUrl(_callId: string | null): string | null {
  return null;
}
